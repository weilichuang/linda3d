需要处理的部分：
  1 Bugs:
       线段渲染裁剪错误. 
  2 Improve：
       一个物体内部有多个物体，并且有透明和不透明的时候，重复渲染的问题。 	
       寻找计算颜色分量最快的方法.                             
       减少浮点数到整数的转换.
	   Matrix4添加一个剪切变形的工具
	   添加四元数支持
	   优化光栅化三角形
	   添加阴影 (0.1版本的特性）
	   添加更多相机类型,添加更好的操作方式
	   是否需要添加一种TextureFlat模式,可以用来添加自发光,并不需要光照
	   Material添加一个是否使用ZBuffer的功能
	   使用新的读取3ds格式Loader,可以读取更多的东西
	   灯光添加范围控制,超过范围的点不光照(并简化计算)

已完成部分：

-添加了线段渲染模式，但目前存在裁剪错误.

-修改了贴图模式,使用了Vector<UInt>代替BitmapData.

-使用了新的选择MipMap和正确贴图透视方式，速度更快、更美观,根据物体的distance来判断是否使用何种贴图.

-修正了MeshBufferSceneNode中，当useDefaultMaterial为true时，物体判断透明度错误的问题。

-发现Vector使用嵌套循环会变的很慢，尽量避免使用嵌套。

-优化了光照计算，去掉了一些不必要的部分，优化了部分计算。

-MeshBuffer添加了几个方法scale(),rotate(),translate()用于改变模型的原始形状,使用该方法前请一定要明白你在做什么。

-使用了新的光栅化方法,不再出现斑点了.

-材质添加了一个正面剔除功能