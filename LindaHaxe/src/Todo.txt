需要处理的部分：
  1 Bugs:
       线段渲染裁剪错误.
       修改MD2MeshFileLoader.
	   测试外部模型导入的部分.
       MD2相关类需要整理压缩(目前解析速度超慢,真是奇怪），还有其中点的模可能要动态计算
  2 Improve：
       一个物体内部有多个物体，并且有透明和不透明的时候，重复渲染的问题。
       测试一种新的三角形光栅化算法，看其速度与现在的这个相比哪个快.  	
       寻找计算颜色分量最快的方法.                             
       减少浮点数到整数的转换.
	   去掉一些不必要的光照

已完成部分：

-添加了线段渲染模式，但目前存在裁剪错误.

-修改了贴图模式,使用了Vector<UInt>代替BitmapData.

-使用了新的选择MipMap和正确贴图透视方式，速度更快、更美观,根据物体的distance来判断是否使用何种贴图.

-修正了MeshBufferSceneNode中，当useDefaultMaterial为true时，物体判断透明度错误的问题。